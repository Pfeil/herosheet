[package]
name = "herosheet"
version = "0.1.0"
authors = ["Andreas Pfeil"]
edition = "2018"

[dependencies]
rocket = "0.5.0-rc.1"
rocket_dyn_templates = { version = "0.1.0-rc.1", features = ["tera"]}
rand = "*"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

anyhow = "1.0"
